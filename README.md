Николаев Артём Александрович, 4 курс

```bash
# клонируем репозиторий
git clone https://github.com/BiologX/Practice.git

# переходим в репозиторий
cd Practice

# создаём ветку и переключаемся на неё
git checkout -b task/homework-1

# закидываем файлы и затем отслеживаем
git add .

# коммитим
git commit -m "Add solution"

# первая отправка новой ветки на удаленный репозиторий с установкой upstream связи
git push origin -u task/homework-1

// переключаемся на главную ветку
git checkout main

git checkout -b task/homework-2
git add Program.cs
git commit -m "Add solution"
git push origin -u task/homework-2
git checkout main

git checkout -b task/homework-3
git add .
git commit -m "Add solution"
git push origin -u task/homework-3
git checkout main

git checkout -b task/homework-4
git add .
git commit -m "Add solution"
git push origin -u task/homework-4
git checkout main

git checkout -b task/homework-5
git add .
git commit -m "Add solution"
git push origin -u task/homework-5
git checkout main

git checkout -b task/homework-6
git add .
git commit -m "Add solution"
git push origin -u task/homework-6
git checkout main

git checkout -b task/homework-7
git add .
git commit -m "Add solution"
git push origin -u task/homework-7
git checkout main

git checkout -b task/homework-8
git add .
git commit -m "Add solution"
git push origin -u task/homework-8
git checkout main

git add .
git commit -m "Edit README.md file"
git push
```
